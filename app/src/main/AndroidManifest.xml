<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.check.viewdraghelper">


    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />



    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ElasticActivity" />

        <activity android:name=".drag.DragActivity" />

        <activity android:name=".LoadAnimActivity" />

        <activity android:name=".RecycViewActivity" />

        <activity android:name=".TabLayoutRecycActivity" />

        <activity android:name=".FileUploadDownLoadActivity"/>

        <activity android:name=".customerboom.BoomActivity" />

        <activity android:name=".pathanim.PathAnimActivity" />





    </application>

</manifest>